{
  "compilerOptions": {
    "target": "es5",    // 指定js版本为es5
    "module": "CommonJS",    // 指定一套js规范
    "moduleResolution": "node",    // 选择模块解析策略
    "experimentalDecorators": true,    // 开启ts装饰器
    "allowSyntheticDefaultImports": true,    // 允许从没有默认导出的模块当中默认导入
    "sourceMap": true,    // 导出map文件，在控制台显示此处代码行数，方便调试
    "strict": true,    // 严格模式
    "noImplicitAny": true,    // 不允许默认的any类型
    "alwaysStrict": true,     // 允许每个文件使用严格模式'use strict'
    "declaration": true,      // 生成对应的.d.ts文件(声明文件)
    "removeComments": true,   // 删除编译后的所有注释
    "noImplicitReturns": true,// 不是函数的所有返回路径都有返回值时报错
    "importHelpers": true,    // 导入辅助工具
    "lib": ["es6", "dom"],    // 指定要包含在编译中的库文件
    "typeRoots": ["node_modules/@types"],    // 编译时要用到的声明文件统一去node_modules文件夹下面找
    "outDir": "./dist",       // 编译之后的文件出口
    "rootDir": "./src"
  },
  "include": [
    "./src/**/*.ts"
  ],
  "exclude": [
    "node_modules",
    "dis"
  ]
}